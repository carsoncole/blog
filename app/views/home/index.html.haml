#articles
  - @posts.each do |post|

    .row
      .col-md-7
        %article
          %h1= link_to post.title, post

          - if post.image.representable?
            = image_tag post.image.variant(resize_to_limit: [100, nil])

          %p= post.content.to_plain_text.truncate(275)
          %p.read-more= link_to "Continue reading", post

          %p.date= post.date
