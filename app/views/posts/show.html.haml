#post

  .row
    .col-md-8
      %h1= @post.title

      .author= @post.author

      %p= @post.content


      %br
      %p.date= @post.date.strftime("%B %d, %Y")

/ = link_to 'Edit', edit_post_path(@post)
/ \|
/ = link_to 'Back', posts_path
